{
  "name": "ai-agents-webapp-worker",
  "version": "1.0.0",
  "description": "Serverless backend for AI Agents Webapp",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build:worker": "esbuild src/index.js --bundle --outfile=dist/worker.js --format=esm",
    "test": "jest"
  },
  "dependencies": {
    "itty-router": "^4.0.23",
    "itty-cors": "^0.3.6",
    "openai": "^4.20.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "jest": "^29.7.0",
    "wrangler": "^3.15.0"
  }
}
